# Проект использует только стандартную библиотеку Python (3.9+):
# http.server, urllib.parse, os, traceback.
# Внешние зависимости не требуются — pip install ничего не установит,
# файл добавлен для полноты структуры проекта / CI-проверок.
